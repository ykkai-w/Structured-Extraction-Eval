# 数据与隐私

示例数据采用合成文本。处理公告、合同、简历、客服记录或其他实际材料时，应确认数据来源允许相应的采集、处理与传播方式。

运行结果会保存模型回答、模型标识、耗时和输入哈希，但默认不复制原始文本。哈希不能替代访问控制，短文本或可枚举文本仍可能被猜测。含敏感信息的数据与结果应保存在访问受限的位置，不应提交到公开仓库。

密钥只通过环境变量读取。`.env`、`private_data/` 与 `outputs/` 已加入 `.gitignore`，但仍需在提交前检查版本记录。若密钥曾被写入受版本控制的文件，应立即在服务端撤销并更换。
